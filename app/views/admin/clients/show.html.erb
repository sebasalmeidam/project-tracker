<h1>
	<span><%= @client.name %></span>	
	<span><%= link_to t('buttons.back'), admin_clients_path, class: "btn btn-default" %></span>
</h1>
<h2>
	<span><%= t('titles.work_done_list') %></span>	
	<span><%= link_to t('buttons.new'), new_admin_client_task_path(@client), class: "btn btn-success" %></span>
</h2>

<div class="row topMargin30">
	<div id="flash"> </div>
	<div class="col-sm-10 col-sm-offset-1" id="client_task_list">
		<%= render 'shared/client_task_list' %>
	</div>
</div>

