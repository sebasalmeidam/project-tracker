<h4><%= t('attributes.add_attachments') %></h4>

<div class= "small"	id="flash"> </div>
	

<%= form_for @attachment, url: {controller: "attachments", action: "create", client_id: @task.client_id, task_id: @task.id }, :html => { :multipart => true, id: "form" }, remote: true do |f| %>
	
	<%= f.hidden_field :client_id, value: @task.client_id %>
	<%= f.hidden_field :task_id, value: @task.id %>

	<div class="form-group row">
		<%= f.label :file_name, "Descripción:", class: "col-sm-1 col-form-label" %>
		<div class="col-md-6">
			<%= f.text_field :file_name, class: "form-control", id: "attachment_file_name" %>
		</div>
	</div>

	<div class="form-group row">
	    <%= f.label :file, "Adjunto:", class: "col-sm-1 col-form-label" %>
	    <div class="col-md-6">
	      <%= f.file_field :file, class: "form-control", id: "attachment_file" %>
	    </div>
	</div>

	<div class="form-group row">
	  <div class="col-sm-1 col-md-offset-1">
	  	<%= f.submit t('buttons.save'), :class => 'btn btn-default', id: "save" %>
		</div>
	</div>

<% end %>



